import styled from "styled-components";

export const DiscountStyled = styled.div`
  position:absolute;
  right: 15px;
  bottom: 32px;
  width: 50px;
  height: 50px;
  border-radius: 50%;
  color: white;
  background-color: var(--chakra-colors-giza-900); 
  filter: drop-shadow(0 0 0.10rem var(--chakra-colors-giza-900));
  font-weight: 500;
  display: grid;
  place-items: center;
`